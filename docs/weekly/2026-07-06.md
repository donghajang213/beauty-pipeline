# 주간 회고 — 2026-07-06 (1주차)

## 한 일
- 기획안(PLAN.md)·작업 규칙(RULES.md) 작성, 주제를 "피부타입별 화장품 추천 플랫폼"으로 확정
- GCP 부트스트랩: 프로젝트 복구, 결제 연결, API 활성화, 예산 알림(월 ₩216,000, gross 기준)
- Terraform으로 데이터 레이크 버킷 + BigQuery raw + 수집용 SA 생성 (PR #1)
- 공공데이터포털 수집 클라이언트/탐침 도구 + 단위 테스트 4건 (PR #2)
- 식약처 API 3종(원료성분 21,833 / 규제 7,257 / 제조업체 32,307건) 실호출 검증

## 배운 것
- 공공데이터포털은 계정당 인증키 1개 — API마다 다른 건 엔드포인트뿐 (활용신청 = 허가 목록 추가)
- .gitignore는 패턴 뒤 인라인 주석 금지, `.env.*` 패턴이 `.env.example`까지 삼킴 → `!` 예외
- 실키는 `.env`에만 — `.env.example`은 커밋되는 빈 양식 (하마터면 키 커밋할 뻔)
- Terraform state의 닭-달걀 문제: state 버킷만은 수동 생성 후 문서화

## 다음 주
- PR #1, #2 병합 → fetch 스크립트 (3개 API → GCS raw, 날짜 파티션, 멱등성)
- 제품 단위 데이터(기능성화장품 계열 API) 추가 신청 검토
- GitHub Actions 기본 CI (ruff + pytest)

## 비용
- 이번 주 지출: ₩0 (버킷/데이터셋은 존재만으로 과금 없음)
